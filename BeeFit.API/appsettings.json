{
  // JWT Token is in user's secrets saved on local machine, but only in Development mode (launchsettings.json)
  "AppSettings": {
    "Token": "blablablablablab"
  },

  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost\\SQLEXPRESS;Database=BeeFit;Trusted_Connection=True;"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
}
